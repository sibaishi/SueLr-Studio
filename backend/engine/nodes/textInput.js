// ============================================================
// Flow Studio - 文本输入节点执行器
// ============================================================

/**
 * 执行文本输入节点
 * 直接返回节点中存储的文本内容
 */
export async function execute(node, inputs) {
  const text = node.data?.text || '';
  return { text };
}
